meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm install --include=dev && npm run build

run:
  command: cd /app/backend && npx prisma db push && node dist/index.js
  persistenceMount: /data
  containerPort: 3000
